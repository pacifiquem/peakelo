# `@peakelo/client`

Next.js App Router UI. AlignUI is the component library. Visual rules live in
`docs/design/ui.md`. Use the `align-ui` skill before adding components.

```bash
pnpm --filter @peakelo/client dev
```
